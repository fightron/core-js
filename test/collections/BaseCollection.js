'use strict'

const expect = require('chai').expect
const BaseCollection = require('../../collections/BaseCollection')

describe('collections/BaseCollection', function () {
  // REPLACE WITH REAL TESTS
  it('exists', function () {
    expect(BaseCollection).to.exist()
  })
})
