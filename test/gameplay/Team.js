'use strict'

const expect = require('chai').expect
const Team = require('../../gameplay/Team')

describe('gameplay/Team', function () {
  // REPLACE WITH REAL TESTS
  it('exists', function () {
    expect(Team).to.exist()
  })
})
